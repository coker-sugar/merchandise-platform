// 创建loginDataType类型并导出
export type loginDataType = {
    username: string;
    password: string;
};

// 创建registerDataType类型并导出
export type userInfoType = {
    username: string;
    password: string;
    email: string;
}

